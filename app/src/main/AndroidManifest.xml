<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vincenzo.testfragmentsezioni">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="android.permission.INTERACT_ACROSS_USERS_FULL"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Home"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AreaUtente"
            android:label="@string/name_area_utente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Registrazione"
            android:label="@string/name_registrazione" />
        <activity
            android:name=".Modifica_dati"
            android:label="@string/name_mod_dati" />
        <activity
            android:name=".OffriPassaggio"
            android:label="@string/name_offri_passaggio"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PassaggioRitorno"
            android:label="@string/name_passaggio_ritorno">

            <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        </activity>
        <activity
            android:name=".Le_mie_offerte"
            android:label="@string/le_mie_offerte">

            <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        </activity>
        <activity
            android:name=".Modifica_offerta"
            android:label="@string/modifica_offerta">

            <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        </activity>
        <activity android:name=".RichiediPassaggio" />
        <activity android:name=".OrganizzaSettimana" />
        <activity android:name=".LeMieRichieste" />
        <activity android:name=".ResultPage" />
        <activity android:name=".ModificaOrari"></activity>
    </application>

</manifest>